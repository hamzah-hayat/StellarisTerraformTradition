@base_tradition_agenda_cost = 7000

# For each sector in the empire, divide number of systems in that sector by five, with a min of three.
# For that many times, pick a random un-habitable planet in a random system in that sector and make it habitable.
agenda_new_worlds = {
	agenda_cost = @base_tradition_agenda_cost
	allow = {
		has_tradition = tr_terraform_adopt
	}

	potential = {
		has_paragon_dlc = yes
	}
	
	effect = {
		custom_tooltip = tt_new_worlds_terraform_planets
		hidden_effect = {
			country_event = { id = tt_events.1 }
		}
	}
}